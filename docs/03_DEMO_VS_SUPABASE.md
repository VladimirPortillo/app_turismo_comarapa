# Supabase

La aplicación usa siempre `SupabaseRegistroRepository` y muestra la pantalla
de inicio de sesión al ejecutarse.

La configuración se carga de `config/local.json`, que se empaqueta como un
asset de Flutter. Para ejecutarla basta con:

`flutter run`
